# Railway deployment configuration for Sales Call Analyzer
# This file tells Railway to build from the repo root with the correct Dockerfile

[build]
dockerfilePath = "sales-call-analyzer/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

