# AI Call Analyzer - Environment Variables
# Copy this file to .env and fill in your values

# Flask (required)
SECRET_KEY=your-secret-key-change-this-in-production

# OpenAI (required)
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o

# ElevenLabs (required for webhook integration)
ELEVENLABS_API_KEY=your-elevenlabs-api-key
ELEVENLABS_WEBHOOK_SECRET=your-webhook-signing-secret

# Default user for webhook calls (optional)
# Calls received via webhook will be associated with this user
DEFAULT_USER_EMAIL=webhook@company.com

# Email - Option 1: Resend (recommended)
RESEND_API_KEY=re_...
RESEND_FROM=noreply@yourdomain.com

# Email - Option 2: SMTP
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=your-email@gmail.com

# Authorization (optional)
# Comma-separated list of allowed email addresses
# If not set, all emails are allowed (dev mode)
ALLOWED_EMAILS=trainer1@company.com,trainer2@company.com

# Magic link expiry (optional, default: 15)
MAGIC_LINK_EXPIRY_MINUTES=15

# App URL (for magic links and webhook configuration)
APP_URL=http://localhost:5000

# Output directory for generated PDFs
OUTPUT_DIR=/tmp/sales-call-analyzer

# Database Configuration
# Option 1: PostgreSQL (recommended for production, e.g., Railway)
# Railway automatically provides DATABASE_URL when you add PostgreSQL service
# DATABASE_URL=postgresql://user:password@host:port/database

# Option 2: SQLite (for local development)
# IMPORTANT: In Docker deployments, set this to a path in a persistent volume
# Example: DATABASE_PATH=/data/sales_calls.db
# The directory will be created automatically if it doesn't exist
DATABASE_PATH=sales_calls.db

# Celery Configuration (optional - for distributed processing)
# USE_CELERY=true
# CELERY_BROKER_URL=redis://localhost:6379/0
# CELERY_RESULT_BACKEND=redis://localhost:6379/0
